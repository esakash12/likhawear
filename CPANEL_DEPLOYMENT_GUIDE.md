# Zinnia Bangladesh — cPanel ডেপ্লয়মেন্ট গাইড (বাংলা)

এই গাইডটি অনুসরণ করে যেকোনো কমদামি বা শেয়ার্ড cPanel হোস্টিংয়ে (যেমন: XeonBD, DianaHost, Namecheap, Hostinger ইত্যাদি) আপনি সম্পূর্ণ সাইটটি খুব সহজে ৫-১০ মিনিটের মধ্যে লাইভ করতে পারবেন। কোনো Node.js বা SSH টার্মিনাল অ্যাক্সেসের প্রয়োজন নেই।

---

## ১. cPanel-এ MySQL ডেটাবেজ তৈরি করুন

1. আপনার হোস্টিংয়ের **cPanel**-এ লগইন করুন।
2. **Databases** সেকশনে গিয়ে **MySQL® Databases**-এ ক্লিক করুন।
3. **Create New Database** বক্সে একটি নাম দিন (যেমন: `zinnia_db`) এবং **Create Database**-এ ক্লিক করুন।
4. নিচে স্ক্রল করে **Add New User** সেকশনে যান:
   - **Username**: যেমন `zinnia_user`
   - **Password**: শক্তিশালী পাসওয়ার্ড দিন (পাসওয়ার্ডটি সেভ করে রাখুন)।
   - **Create User** বাটনে ক্লিক করুন।
5. আরও নিচে স্ক্রল করে **Add User To Database** সেকশনে যান:
   - তৈরি করা User এবং Database সিলেক্ট করুন এবং **Add** বাটনে ক্লিক করুন।
   - **ALL PRIVILEGES** চেকবক্সে টিক দিন এবং **Make Changes**-এ ক্লিক করুন।

---

## ২. phpMyAdmin দিয়ে `database.sql` ইম্পোর্ট করুন

1. cPanel ড্যাশবোর্ডে ফিরে গিয়ে **phpMyAdmin** ওপেন করুন।
2. বাম পাশের তালিকা থেকে আপনার তৈরি করা ডেটাবেজে ক্লিক করুন।
3. উপরের মেনু থেকে **Import** ট্যাবে ক্লিক করুন।
4. **Choose File** বাটনে ক্লিক করে প্রজেক্ট ফোল্ডারে থাকা `database.sql` ফাইলটি সিলেক্ট করুন।
5. নিচে গিয়ে **Import** বা **Go** বাটনে ক্লিক করুন।
   - মাত্র ২-৩ সেকেন্ডে সমস্ত টেবিল (Products, Categories, Orders, Coupons, CMS, Admin User) ডেটাবেজে প্রবেশ করবে।

---

## ৩. ফাইল আপলোড ও এক্সট্র্যাক্ট করুন

1. আপনার কম্পিউটারে এই প্রজেক্ট ফোল্ডারটি একটি `.zip` ফাইল হিসেবে জিপ করুন (লক্ষ্য রাখুন: `node_modules` বাদ দিয়ে জিপ করতে পারেন, কারণ প্রজেক্ট বিল্ড করা অ্যাসেটগুলো আগেই `public/assets/`-এ রেডি আছে)।
2. cPanel-এর **File Manager** ওপেন করুন।
3. **public_html** ফোল্ডারে প্রবেশ করুন।
4. উপরের **Upload** বাটনে ক্লিক করে তৈরি করা `.zip` ফাইলটি আপলোড করুন।
5. আপলোড শেষ হলে ফাইলটি সিলেক্ট করে **Extract**-এ ক্লিক করুন।
6. রুট ডিরেক্টরিতে থাকা `.htaccess` ফাইলটি স্বয়ংক্রিয়ভাবে সমস্ত রিকোয়েস্ট নিরাপদভাবে `public/` ফোল্ডারে পাঠিয়ে দেবে এবং সেনসিটিভ ফাইল (`.env`, `storage` ইত্যাদি) সাধারণ ভিজিটরদের ব্রাউজার থেকে সম্পূর্ণ ব্লক রাখবে।

---

## ৪. `.env` ফাইলে ডেটাবেজ কনফিগারেশন বসান

1. cPanel File Manager-এ যদি ডট (`.`) ফাইল না দেখা যায়, তবে উপরের ডান কোনায় **Settings** গিয়ার আইকনে ক্লিক করে **Show Hidden Files (dotfiles)** অন করুন।
2. রুট ফোল্ডারে `.env` ফাইলটি সিলেক্ট করে **Edit**-এ ক্লিক করুন।
3. ডেটাবেজ অংশে আপনার cPanel-এর তথ্যগুলো বসিয়ে দিন:

```env
APP_NAME="Zinnia Bangladesh"
APP_ENV=production
APP_KEY=base64:M5mboTDrIAXXP6aRTwWFQ1VAqjA0//XfcP1iAjNqyoI=
APP_DEBUG=false
APP_URL=https://yourdomain.com

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=yourcpaneluser_zinnia_db
DB_USERNAME=yourcpaneluser_zinnia_user
DB_PASSWORD=YourDatabasePasswordHere

SESSION_DRIVER=file
CACHE_STORE=file
```

4. **Save Changes**-এ ক্লিক করে ফাইলটি সংরক্ষণ করুন।

---

## ৫. ফোল্ডার পারমিশন চেক করুন

File Manager-এ নিচের ফোল্ডারগুলোর পারমিশন `755` বা `775` রয়েছে কিনা নিশ্চিত করুন:
- `storage` (এবং এর ভিতরের সব সাব-ফোল্ডার)
- `bootstrap/cache`
- `public/uploads`

---

## ৬. সিক্রেট অ্যাডমিন লগইন তথ্য (Stealth Admin URL)

সাইটের মূল ইন্টারফেসে (Header / Footer / Mobile Menu) কোনো অ্যাডমিন লিংক রাখা হয়নি। শুধুমাত্র সিক্রেট ইউআরএল টাইপ করলেই অ্যাডমিন লগইন স্ক্রিন আসবে:

- **Secret Admin URL**: `https://yourdomain.com/mg/shohag/admin`
  *(লোকালি চেক করার জন্য: `http://127.0.0.1:8085/mg/shohag/admin`)*
- **Email**: `admin@zinniabd.com`
- **Password**: `admin123456`

> **সিকিউরিটি নোট:**
> কোনো বট বা হ্যাকার যদি `/admin` বা `/admin-login` দিয়ে স্ক্যান করে, তবে তাকে স্বয়ংক্রিয়ভাবে মূল হোমপেজে পাঠিয়ে দেওয়া হবে। ফলে অ্যাডমিন প্যানেলের অস্তিত্ব পুরোপুরি গোপন থাকবে।

> **পাসওয়ার্ড পরিবর্তন:**
> অ্যাডমিন প্যানেলে লগইন করার পর কন্ট্রোল সেন্টারের প্রোফাইল/পাসওয়ার্ড অপশন থেকে পাসওয়ার্ড পরিবর্তন করে নিতে পারবেন। অথবা phpMyAdmin-এর `users` টেবিলে গিয়ে সরাসরি পাসওয়ার্ড হ্যাশ আপডেট করা যায়।

---

## ৭. ক্লায়েন্টের জন্য মূল সুবিধাসমূহ

1. **কোনো Node.js ইন্সটলেশনের ঝামেলা নেই:** সস্তা ১ জিবি র‍্যামের শেয়ার্ড হোস্টিংয়েও সাইট পলকের মধ্যে লোড হবে।
2. **রিয়েল ডেটাবেজ কানেক্টিভিটি:** প্রডাক্ট স্টক, অর্ডার বুকিং, কুপন ডিসকাউন্ট, ট্র্যাকিং স্ট্যাটাস—সবকিছু সরাসরি MySQL ডেটাবেজে সংরক্ষিত হচ্ছে।
3. **সার্ভারে ছবি আপলোড:** অ্যাডমিন প্যানেল থেকে প্রডাক্ট যুক্ত করার সময় সরাসরি ডিভাইস থেকে ছবি আপলোড করা যাবে, যা হোস্টিংয়ের `public/uploads` ফোল্ডারে সেভ হবে।
4. **সম্পূর্ণ সিকিউর:** লারাভেলের টোকেন অথেনটিকেশন (Sanctum), ব্রুট-ফোর্স রেট লিমিটিং, এবং SQL Injection প্রতিরোধী Eloquent ORM সক্রিয়।
